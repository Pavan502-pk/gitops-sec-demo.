apiVersion: v1
kind: Service
metadata:
  name: sample-vulnerable-service
  namespace: default
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
  selector:
    app: vulnerable-app
